---
- name: play 1
  hosts: worker
  gather_facts: false
  tasks:
    - name: git clone "eraserandrain/container" repo
      git:
        repo: https://github.com/EraserandRain/container
        dest: "{{ lookup('env','HOME') }}/container"
        version: main